---
export interface Props {
  coctel: {
    id: string;
    name: string;
    description: string;
    price: string;
    ingredients: string[];
  };
  addToCart: (coctel: { name: string; price: number; uds: number }) => void;
}

const { coctel, addToCart } = Astro.props;

function handleAddToCart() {
  const coctelItem = {
    name: coctel.name,
    price: parseFloat(coctel.price),
    uds: 1
  };
  addToCart(coctelItem);
}
---

<div class="cocktail-card">
  <h3>{coctel.name}</h3>
  <p>{coctel.description}</p>
  <p>Price: {coctel.price}€</p>
  <button onClick={handleAddToCart}>Add to Cart</button>
</div>

<style>
  .cocktail-card {
    background: linear-gradient(135deg, #6a1b9a, #d500f9);
    color: #fff;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s;
    width: 100%;
    cursor: pointer;
  }
  .cocktail-card:hover {
    transform: translateY(-10px);
  }
  .cocktail-card h3 {
    font-size: 1.5rem;
    margin-bottom: 10px;
  }
  .cocktail-card p {
    font-size: 1rem;
    margin-bottom: 10px;
  }
  button {
    background: #ff4081;
    border: none;
    padding: 10px 20px;
    color: #fff;
    border-radius: 5px;
    cursor: pointer;
    transition: background 0.3s;
  }
  button:hover {
    background: #e91e63;
  }
</style>
